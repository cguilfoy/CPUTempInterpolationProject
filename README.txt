# CPUTempInterpolationProject

Instructions:

This program runs by jar via the command prompt. The Jar is located in build/libs but I've placed a copy in the root directory for your ease of use.

Run it via command prompt java -jar CPUTempInterpolationProject.jar <filename of temp readings>. It takes 1 parameter, and only 1 parameter.